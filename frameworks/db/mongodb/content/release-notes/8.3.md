---
type: "Framework Learn Page"
framework: "mongodb"
source_repo: "https://github.com/mongodb/docs.git"
source_branch: "main"
source_path: "content/manual/manual/source/release-notes/8.3.txt"
source_commit: "96788e8ed140cbdde184ff82e1066dff4996bde4"
source_commit_short: "96788e8e"
source_commit_date: "2026-06-19T21:35:03-06:00"
generated_at: "2026-06-21T07:41:52Z"
---

=============================

# Release Notes for MongoDB 8.3

.. include:: /includes/minor-release.rst

## Patch Releases

### 8.3.4 - June 11, 2026

> **Important:** MongoDB 8.3.4 contains a fix for
[CVE-2026-11933](https://www.cve.org/CVERecord?id=CVE-2026-11933)
For the latest information about MongoDB security updates, see
[MongoDB Security Bulletins](https://www.mongodb.com/resources/products/alerts?tck=docs).

Issues fixed:

- :issue:`SERVER-128125` Align bsonObjToArray ownership semantics
with bsonGetImmutable

- `All JIRA issues closed in 8.3.4
<https://jira.mongodb.org/issues/?jql=project%20in%20(SERVER%2CTOOLS%2CWT)%20AND%20resolution%3D%27Fixed%27%20and%20fixversion%3D%278.3.4%27>`_

- `8.3.4-changelog`
### 8.3.3 - June 9, 2026

> **Important:** MongoDB 8.3.3 contains a fix for:
- [CVE-2026-9735](https://www.cve.org/CVERecord?id=CVE-2026-9735)
- [CVE-2026-9740](https://www.cve.org/CVERecord?id=CVE-2026-9740)
- [CVE-2026-9741](https://www.cve.org/CVERecord?id=CVE-2026-9741)
- [CVE-2026-9742](https://www.cve.org/CVERecord?id=CVE-2026-9742)
- [CVE-2026-9746](https://www.cve.org/CVERecord?id=CVE-2026-9746)
- [CVE-2026-9747](https://www.cve.org/CVERecord?id=CVE-2026-9747)
- [CVE-2026-9748](https://www.cve.org/CVERecord?id=CVE-2026-9748)
- [CVE-2026-9749](https://www.cve.org/CVERecord?id=CVE-2026-9749)
- [CVE-2026-9750](https://www.cve.org/CVERecord?id=CVE-2026-9750)
- [CVE-2026-9751](https://www.cve.org/CVERecord?id=CVE-2026-9751)
- [CVE-2026-9752](https://www.cve.org/CVERecord?id=CVE-2026-9752)
- [CVE-2026-9753](https://www.cve.org/CVERecord?id=CVE-2026-9753)
For the latest information about MongoDB security updates, see
[MongoDB Security Bulletins](https://www.mongodb.com/resources/products/alerts?tck=docs).

Issues fixed:

- :issue:`SERVER-115250` Pause JWKSetRefreshJob during quiesce time
- :issue:`SERVER-121822` Clustered collections do not correctly filter out
results with `$lt on id`

- :issue:`SERVER-125887` Add support for encrypted PEM fules for gRPC egress
- :issue:`SERVER-126049` MozJS per-scope heap limit check uses process-global GC
counter

- :issue:`WT-16780` Insufficient error context in `WT_PANIC` checkpoint failures
in `__wt_block_checkpoint_resolve`

- :issue:`SERVER-126506` Remove SASL params from egress logging
- :issue:`SERVER-125063` Prohibit nesting of BSONColumn inside BSONColumn
- :issue:`SERVER-123507` Encrypted predicates inside a `$vectorSearch`
prefilter should get replaced with encryption placeholders

- :issue:`SERVER-124183` Limit scope of authenticate
- :issue:`SERVER-124190` Forbid aggregate `exchange` field from non-internal
clients

- :issue:`SERVER-123918` Validate `runtimeConstants.userRoles` in IDL
- :issue:`SERVER-123951` `$_internalConvertBucketIndexStats` should not return
PauseExecution on failed index conversion

- :issue:`SERVER-124031` Ensure `Exchange::getTargetConsumer` correctly
handles MaxKey

- :issue:`SERVER-123633` Fix handling of user $-prefixed fields colliding with
internal metadata

- :issue:`SERVER-123370` LDAP query password is logged when set through
setParameter

- :issue:`SERVER-123440` GeometryCollection with strict-winding polygon causes
2dsphere index key generation crash

- :issue:`SERVER-124959` Validate doc diff array bounds and
`$_internalApplyOplogUpdate` BSON

- `All JIRA issues closed in 8.3.3
<https://jira.mongodb.org/browse/SERVER-126391?jql=project%20%3D%20SERVER%20AND%20resolution%3D%27Fixed%27%20AND%20fixVersion%20%3D%208.3.3>`_

- `8.3.3-changelog`
Cost-Based Ranker:

- :issue:`SERVER-118434` Makes the cost-based ranker (CBR) eligible for use
during query planning

- :issue:`SERVER-115402` Adds `explain` output to the CBR
- :issue:`SERVER-120369` Adds :dbcommand:`serverStatus` metrics for the CBR
- :issue:`SERVER-117199` Adds :pipeline:`$queryStats` metrics for the CBR
For more information about the CBR, see `query-plan-options`.

### 8.3.2 - May 12, 2026

> **Important:** MongoDB 8.3.2 contains a fix for
[CVE-2026-8053](https://www.cve.org/CVERecord?id=CVE-2026-8053)
For the latest information about MongoDB security updates, see
[MongoDB Security Bulletins](https://www.mongodb.com/resources/products/alerts?tck=docs).

Issues fixed:

- :issue:`SERVER-126021` Undefined behavior when inserting data with
duplicate field names into timeseries collections

- `All JIRA issues closed in 8.3.2
<https://jira.mongodb.org/issues/?jql=project%20in%20(SERVER%2CTOOLS%2CWT)%20AND%20resolution%3D%27Fixed%27%20and%20fixversion%3D%278.3.2%27>`_

- `8.3.2-changelog`
### 8.3.1 - May 4, 2026

> **Important:** MongoDB 8.3.1 contains a fix for:
- [CVE-2026-8199](https://www.cve.org/CVERecord?id=CVE-2026-8199)
- [CVE-2026-8200](https://www.cve.org/CVERecord?id=CVE-2026-8200)
- [CVE-2026-8201](https://www.cve.org/CVERecord?id=CVE-2026-8201)
- [CVE-2026-8202](https://www.cve.org/CVERecord?id=CVE-2026-8202)
- [CVE-2026-8336](https://www.cve.org/CVERecord?id=CVE-2026-8336)
For the latest information about MongoDB security updates, see
[MongoDB Security Bulletins](https://www.mongodb.com/resources/products/alerts?tck=docs).

Issues fixed:

- :issue:`SERVER-121610` Ensure cached `emit()` calls are no-op
- :issue:`SERVER-121895` Improve redaction of validation errors
- :issue:`SERVER-124974` Ensure memory lifetime during `$match`
pushdown analysis

- `All JIRA issues closed in 8.3.1
<https://jira.mongodb.org/issues/?jql=project%20in%20(SERVER%2CTOOLS%2CWT)%20AND%20resolution%3D%27Fixed%27%20and%20fixversion%3D%278.3.1%27>`_

- `8.3.1-changelog`
### 8.3.0 - May 4, 2026

> **Important:** MongoDB 8.3 includes fixes for [CVE-2025-12893](https://www.cve.org/CVERecord?id=CVE-2025-12893),
[CVE-2025-13507](https://www.cve.org/CVERecord?id=CVE-2025-13507),
[CVE-2025-14345](https://www.cve.org/CVERecord?id=CVE-2025-14345), and
[CVE-2026-8336](https://www.cve.org/CVERecord?id=CVE-2026-8336),
[CVE-2026-9754](https://jira.mongodb.org/browse/CVE-220).
For the latest information about MongoDB security updates, see
[MongoDB Security Bulletins](https://www.mongodb.com/resources/products/alerts?tck=docs).

## General Changes

### Accessing Array Element Indexes in $map, $filter, and $reduce

.. include:: /includes/array-element-index.rst

### FTDC Statistics

Starting in MongoDB 8.3, `self-managed-ftdc` (FTDC) collects :dbcommand:`connPoolStats` metrics for :binary:`mongod` processes.

### serverStatus Output

:dbcommand:`serverStatus` includes the following new fields in its output:

- `aggregate.withExtension` sub-field in `metrics.commands.aggregate`, tracking success and failure counts
- :serverstatus:`shardingStatistics.databaseCriticalSectionStatistics`
- :serverstatus:`shardingStatistics.collectionCriticalSectionStatistics`
- :serverstatus:`shardingStatistics.databaseVersionUpdateCounters`
- :serverstatus:`metrics.query.cbr`
- :serverstatus:`metrics.query.cbr.choseWinningPlan`
- :serverstatus:`metrics.query.cbr.count`
- :serverstatus:`metrics.query.cbr.histograms`
- :serverstatus:`metrics.query.cbr.histograms.micros`
- :serverstatus:`metrics.query.cbr.histograms.numPlans`
- :serverstatus:`metrics.query.cbr.histograms.samplingMicros`
- :serverstatus:`metrics.query.cbr.micros`
- :serverstatus:`metrics.query.cbr.numPlans`
- :serverstatus:`metrics.query.cbr.numPlansFailedCostEstimation`
- :serverstatus:`metrics.query.cbr.numPlansTiedCostEstimation`
- :serverstatus:`metrics.query.cbr.samplingMicros`
- :serverstatus:`metrics.ttl.deletedKeys`
- :serverstatus:`metrics.ttl.durationMicros`
- :serverstatus:`metrics.ttl.examinedDocuments`
- :serverstatus:`metrics.ttl.examinedKeys`
- :serverstatus:`metrics.extension`
- :serverstatus:`metrics.extension.totalAggStageExecMicros`
- :serverstatus:`metrics.extension.apiCallSuccesses`
- :serverstatus:`metrics.extension.apiCallFailures`
- :serverstatus:`metrics.extension.vectorSearch`
- :serverstatus:`metrics.extension.vectorSearch.legacyVectorSearchUsed`
- :serverstatus:`metrics.extension.vectorSearch.extensionVectorSearchUsed`
- :serverstatus:`metrics.extension.vectorSearch.onViewKickbackRetries`
- :serverstatus:`metrics.extension.vectorSearch.inSubpipelineKickbackRetries`
- :serverstatus:`metrics.repl.network.oplogFetcherLagSeconds`
- :serverstatus:`metrics.repl.network.oplogGetMoresProcessed`
- :serverstatus:`queues.execution.admissions`
- :serverstatus:`queues.execution.backgroundTasksDeprioritization`
- :serverstatus:`queues.execution.deprioritizationGate`
- :serverstatus:`queues.execution.heuristicDeprioritization`
- :serverstatus:`queues.execution.heuristicDeprioritizationThreshold`
- :serverstatus:`queues.execution.totalDeprioritizations`
- :serverstatus:`queues.execution.usesPrioritization`
- :serverstatus:`queues.execution.usesThroughputProbing`
- :serverstatus:`queues.execution.nonDeprioritizable`
- :serverstatus:`queues.execution.deprioritizable`
- :serverstatus:`queues.execution.read.normalPriority.totalDelinquentAcquisitions <normalPriority.totalDelinquentAcquisitions>`
- :serverstatus:`queues.execution.read.normalPriority.totalAcquisitionDelinquencyMillis <normalPriority.totalAcquisitionDelinquencyMillis>`
- :serverstatus:`queues.execution.read.normalPriority.maxAcquisitionDelinquencyMillis <normalPriority.maxAcquisitionDelinquencyMillis>`
- :serverstatus:`queues.execution.read.lowPriority <lowPriority>`
MongoDB 8.3 removes the `service` field in the :dbcommand:`serverStatus` output.

.. include:: /includes/fact-serverstatus-excl-fields-w-none.rst

For details, see `serverstatus-excl-fields`.

You can also optionally specify `lockContentionMetrics: 1` in the :dbcommand:`serverStatus` command to include `server-status-lockContentionMetrics` in the output.

### String Conversion Enhancements

Starting in MongoDB 8.3, :expression:`$toString` can convert additional BSON types to strings, including objects, arrays, regular expressions, MaxKeys, MinKeys, and timestamps. For details, see :expression:`$toString`.

MongoDB 8.3 introduces the optional `base` argument for :expression:`$convert` to convert between string and numeric values using bases `2`, `8`, `10`, and `16`. For details, see :expression:`$convert`.

### Array Conversion Enhancements

Starting in MongoDB 8.3, you can convert strings that represent arrays using :expression:`$convert` or the shorthand expression :expression:`$toArray`.

You can also convert between `binData` and arrays of numeric values. For details, see `convert-bindata-to-array` and `Convert an Array of Numeric Values to binData <convert-array-of-numeric-values-to-bindata>`.

### Object Conversion Enhancements

Starting in MongoDB 8.3, you can convert strings that represent objects using :expression:`$convert` or the shorthand expression :expression:`$toObject`.

### Geospatial Index Generation

Starting in MongoDB 8.3, if a document contains both GeoJSON and legacy numeric coordinate fields, geospatial index generation prioritizes GeoJSON coordinates.

If your documents contain legacy numeric coordinate values that precede GeoJSON coordinates and existing indexes rely on that behavior, you might need to rebuild your geospatial indexes.

This change may require you to rebuild geospatial indexes if your documents contain legacy numeric coordinate values that precede GeoJSON coordinates and existing indexes rely on that behavior. Review geospatial queries to confirm they return expected results.

### Remove Shard Changes

Starting in MongoDB 8.3, the :dbcommand:`removeShard` command is deprecated. This release introduces four new commands to replace `removeShard` and provide granular control over the shard draining and removal process.

- :dbcommand:`startShardDraining`
- :dbcommand:`stopShardDraining`
- :dbcommand:`shardDrainingStatus`
- :dbcommand:`commitShardRemoval`
MongoDB provides four additional commands to transition from an embedded config server to a dedicated config server.

- :dbcommand:`startTransitionToDedicatedConfigServer`
- :dbcommand:`stopTransitionToDedicatedConfigServer`
- :dbcommand:`getTransitionToDedicatedConfigServerStatus`
- :dbcommand:`commitTransitionToDedicatedConfigServer`
### PreAuth Connection Limit Parameters

MongoDB 8.3 adds three new server parameters that limit the resource consumption of pre-auth connections:

- :parameter:`capMemoryConsumptionForPreAuthBuffers`
- :parameter:`messageSizeErrorRateSec`
- :parameter:`preAuthMaximumMessageSizeBytes`
### cachePressureQueryPeriodMilliseconds Parameter

MongoDB 8.3 adds the `cachePressureQueryPeriodMilliseconds` parameter which sets the time interval MongoDB waits between checks for storage engine cache pressure. If the storage engine is under cache pressure, MongoDB aborts the oldest transaction. For details, see :parameter:`cachePressureQueryPeriodMilliseconds`.

### diagnosticDataCollectionDirectorySizeMB Parameter

MongoDB 8.3 increases the default value of :parameter:`diagnosticDataCollectionDirectorySizeMB` from `200` to `500` MiB.

### overloadAwareServerSelectionEnabled Parameter

MongoDB 8.3 adds the :parameter:`overloadAwareServerSelectionEnabled` parameter, which controls whether retries that target other servers in the cluster avoid members that recently returned an error labeled `SystemOverloadedError`. You can enable this parameter to opt in to overload-aware retargeting.

### ingressRequestRateLimiterApplicationExemptions Parameter

Starting in MongoDB 8.3, the `ingressRequestRateLimiterApplicationExemptions` parameter lets you specify applications to exempt from ingress request rate limiting. For details, see :parameter:`ingressRequestRateLimiterApplicationExemptions`.

### Database Profiler Parameters and Metrics

MongoDB 8.3 adds two parameters that let you control the impact that the `database-profiler` has on query performance:

- :parameter:`internalQueryGlobalProfilingLockDeadlineMs`
- :parameter:`internalProfilingMaxAbandonedWritesPerSecondPerDb`
MongoDB 8.3 also adds two related `serverStatus` metrics:

- :serverstatus:`profiler.totalAbandonedWrites`
- :serverstatus:`profiler.dbsPastThreshold`
### Retry Parameters

MongoDB 8.3 adds parameters to configure how internal server-side clients (between :program:`mongos` and :program:`mongod`) back off when retrying operations that fail due to overload.

- :parameter:`defaultClientBaseBackoffMillis`
- :parameter:`defaultClientMaxBackoffMillis`
- :parameter:`defaultClientRetryAttempts`
- :parameter:`shardRetryTokenBucketCapacity`
- :parameter:`shardRetryTokenReturnRate`
### BSONObjectTooLarge Code for Upsert Errors

Starting in 8.3, upsert operations that produce a `BSON object <bson-types>` that is too large return the :error:`10334` `BSONObjectTooLarge` error code. In earlier versions, upsert operations that produce a BSON object that is too large return the `17419` or `17420` error codes.

### validate Command Updates

Starting in 8.3, MongoDB no longer automatically enables the `checkBSONConformance` field in the :method:`db.collection.validate()` method when `full` is `true`. Additionally, the `db.collection.validate()` method now checks to ensure a collection doesn't have any documents that exceed 16 MB.

### New Expression $subtype

MongoDB 8.3 introduces a new :expression:`$subtype` expression to extract the subtype of binary data objects.

### New Expressions for Extended JSON Conversion

MongoDB 8.3 introduces new aggregation expressions for converting between BSON and Extended JSON (EJSON) format:

- :expression:`$serializeEJSON` - Converts BSON values to Extended JSON format
with type wrappers. Supports both Canonical and Relaxed Extended JSON formats.

- :expression:`$deserializeEJSON` - Converts Extended JSON format back
to native BSON values.

These expressions can be combined with :expression:`$convert` to parse JSON strings, transform Extended JSON values into native BSON values, and convert back to JSON format.

### New Expression $createObjectId

Starting in MongoDB 8.3, you can use :expression:`$createObjectId` to generate new random :method:`ObjectId` values in pipelines and expression-based updates.

### New Hashing Expressions

MongoDB 8.3 introduces two new aggregation expressions for generating hashes from UTF-8 strings or binary data:

- :expression:`$hash` - Generates a :bsontype:`BinData <Binary>`
hash value from a UTF-8 string or binary data using MD5, SHA-256, or XXH64.

- :expression:`$hexHash` - Generates an uppercase hexadecimal
string hash from a UTF-8 string or binary data using MD5, SHA-256, or XXH64.

### New $meta Keyword

MongoDB 8.3 introduces a new `"searchRootDocumentId"` keyword for the :expression:`$meta expression. You can use this keyword to return the id` value of the root document for embedded documents in the :pipeline:`$search` result set. To learn more, see `searchRootDocumentId <fts-return-scope>`.

### Pre-Epoch Dates

Starting in MongoDB 8.3, if `unit` is not `"millisecond"` in :expression:`$dateAdd` or :expression:`$dateSubtract` and the input date is before `ISODate("1970-01-01T00:00:00Z")`, the result will be one second greater than in previous versions of MongoDB. This also affects the results of aggregations that perform time-based calculations like :pipeline:`$setWindowFields` and :pipeline:`$densify`.

### Geospatial Index Generation

MongoDB 8.3, continues the behavior introduced in MongoDB 8.2. If a document contains both GeoJSON and legacy numeric coordinate fields, geospatial index generation prioritizes GeoJSON coordinates.

If your documents contain legacy numeric coordinate values that precede GeoJSON coordinates and existing indexes rely on that behavior, you might need to rebuild your geospatial indexes.

This change might require you to rebuild geospatial indexes if your documents contain legacy numeric coordinate values that precede GeoJSON coordinates and existing indexes rely on that behavior. Review geospatial queries to confirm they return expected results.

### Subnormal Numbers in $convert Pipelines

Starting in MongoDB 8.3, the server is able to parse the full range of all representable double precision floating point numbers. This includes subnormal numbers where the most significant digit has leading zeroes and the exponent has the least possible value.

For details, see `subnormal-numbers-parse-behavior-convert`.

### chars Length Limit for $trim, $ltrim, and $rtrim

Starting in MongoDB 8.3, the length of the string you provide to `chars` is limited to 4096 characters. For details, see `$trim Limitations <trim-limitations>`.

### Window Functions

Starting in MongoDB 8.3, you can use :expression:`$mergeObjects` in :pipeline:`$setWindowFields` operations.

### sp.process() limit Option

Starting in MongoDB 8.3, :method:`sp.process()` supports the `options.limit` input parameter. After `sp.process()` returns `limit` documents to your terminal, the session ends.

### Text Search Spilling in Query Engine

.. include:: /includes/fact-text-search-spilling.rst

### chars Length Limit for $trim, $ltrim, and $rtrim

Starting in MongoDB 8.3, the length of the string you provide to `chars` is limited to 4096 characters. For details, see `$trim Limitations <trim-limitations>`.

## Sharding

### DDL Operations for Sharded Clusters Can Only Run on mongos

.. include:: /includes/fact-direct-shard-ddls-ops.rst

### findShardsOnConfigTimeoutMS Parameter

MongoDB 8.3 adds the `findShardsOnConfigTimeoutMS` parameter which sets the timeout in milliseconds for `find` operations against the `config.shards` namespace. For details, see :parameter:`findShardsOnConfigTimeoutMS`.

### config.csrs.indexes Removed

MongoDB 8.3 removes the `config.csrs.indexes` namespace.

### 2dsphereIndexVersion

.. include:: /includes/fact-2dsphereIndexVersion-4.rst

### Shard Query Stats for mongos-Originated Queries

Starting in MongoDB 8.3, shard servers include queries that originate from `mongos` in their query statistics when :serverstatus:`queryStats` is enabled. Previously, most queries forwarded from `mongos` to shards did not appear in shard-level query stats, which reduced the completeness of shard-level diagnostics.

### Conversion to Sharded Clusters

.. include:: /includes/fact-no-shard-after-demotion.rst

### Conversion to Embedded Config Shard

MongoDB 8.3 introduces the :option:`mongod --replicaSetConfigShardMaintenanceMode` option.

In previous releases, converting a replica set to a sharded cluster with an embedded config shard required you to first migrate to a sharded cluster with a dedicated config server replica set.

If you restart a replica set with the `--replicaSetConfigShardMaintenanceMode` and `--configsvr` options, you can convert the primary node directly into an embedded config shard.

For more information, see `convert-replica-set-to-embedded-config-server`.

## Time Series

### refineCollectionShardKey Formatting

Starting in MongoDB 8.3, :dbcommand:`refineCollectionShardKey` for time-series collections only accepts shard keys specified using the collection's logical meta and time fields. MongoDB no longer supports specifying keys using the underlying buckets format.

### Index Naming Restrictions

Starting in MongoDB 8.3, creating an index with the name of `"_id_"` or specifying a hint of `"_id_"` on time series collections returns an error.

### timeField Naming Restrictions

Starting in MongoDB 8.3, you cannot create a `timeField` that starts with a `$` character.

## Logging

### Fields to Track Use of Memory by Queries

Starting in MongoDB 8.3, you can use the following new fields to track query memory use:

Both fields appear in the following places:

- `current-op-agg` output
- :method:`db.currentOp()` output
- `Database profiler <profiler>` output
- `Log messages <log-messages-ref>`
- `Slow query log messages <log-message-json-examples-slow-op>`
The `peakTrackedMemBytes` field also appears in the following places:

- `Explain results <explain-results>`
- :method:`PlanCache.list()` output
- :pipeline:`$planCacheStats` output
Starting in 8.3, use the following new metrics in :pipeline:`$queryStats` to measure the behavior of `checkForInterrupt`:

- `metrics.numInterruptChecksPerSec`
- `metrics.overdueInterruptApproxMaxMillis`
### Mongos Query Shape Field

Starting in MongoDB 8.3, slow query log entries have an optional `originalQueryShapeHash` field that contains that query shape of the following operations that originate on the :program:`mongos` :

- :dbcommand:`find`
- :dbcommand:`aggregate`
- :dbcommand:`count`
- :dbcommand:`distinct`
### Slow In-Progress Query Logs

MongoDB 8.3 introduces slow in-progress query log entries. Log messages on slow in-progress queries contain less information than standard slow query logs. MongoDB logs queries at most once if the query's duration surpasses the `slowOpInProgressThreshold`. You can set the threshold with :method:`db.setProfilingLevel()` or the new :option:`mongod --defaultSlowInProgMS` command line option.

## Explain Output

### Sharded Clusters

Starting in MongoDB 8.3, if you run `explain()` against a database that does not exist on a sharded cluster, the execution stage reaches the end-of-stream and the operation does not create the database. For more information on end-of-stream execution stats, see `explain.executionStats.executionStages.isEOF`.

## Security

### SCRAM-SHA-1 and FIPS Mode

.. include:: /includes/fact-tlsFIPSMode_SCRAM-SHA-1.rst

## Upgrade Procedures

To upgrade to MongoDB 8.3, refer to the upgrade instructions specific to your MongoDB deployment:

- `8.3-upgrade-from-8.0`
- `8.3-upgrade-from-8.2`
If you need guidance on upgrading to 8.3, MongoDB professional services offer support to help ensure a smooth transition without interruption to your MongoDB application. To learn more, see :products:`MongoDB Consulting </consulting>`.

## Download

To download MongoDB 8.3, go to the [MongoDB Download Center](https://www.mongodb.com/try/download/community)_.

.. include:: /includes/admonition-minor-release-features.rst

## Downgrade Considerations

.. include:: /includes/downgrade/previous-version.rst

### Only Single-Version Downgrades are Supported

.. include:: /includes/8.3-upgrade-replacements.rst

.. include:: includes/downgrade/single-version-support-adjacent-versions.rst

### Backward-Incompatible Features

MongoDB 8.3 includes features that are not compatible with earlier releases. Downgrading from 8.3 to an earlier release requires that you remove data that uses these features.

For more information, see `8.3-downgrade-incompatible`.

## Contents

- Compatibility Changes </release-notes/8.3-compatibility>
- Upgrade 8.2 to 8.3 </release-notes/8.3-upgrade>
- Upgrade 8.0 to 8.3 </release-notes/8.3-upgrade-from-8.0>
- Downgrade 8.3 to 8.2 </release-notes/8.3-downgrade>
- Downgrade 8.3 to 8.0 </release-notes/8.3-downgrade-to-8.0>
- Changelog </release-notes/8.3-changelog>
